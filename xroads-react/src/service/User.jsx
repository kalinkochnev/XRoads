const userReducer = (state, action) => {
    switch (action.type) {
        // case 'logged in': Example
        //     return {...state, ...loggedIn()}
        default:
            return state;
    }
}

export { userReducer };